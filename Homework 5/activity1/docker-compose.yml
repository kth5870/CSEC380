version: "3"
services:    
  mysql_db:
    image: mysql
    environment: 
      - MYSQL_ROOT_PASSWORD=Password-123
    build:
      dockerfile: Dockerfile
      context: ./database
    ports: 
      - "3306:3306"

  webserver:
    build:
      dockerfile: Dockerfile
      context: ./web

      
